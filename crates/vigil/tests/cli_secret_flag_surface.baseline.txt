# Frozen secret-shaped command-line flag inventory for every production
# crate's src/: crates/vigil/src, crates/vigil-ha/src, crates/vigil-bin/src.
#
# One record per line: file|flag
#   file - the crate name, then a path relative to that crate's src/
#          (e.g. "vigil/config.rs")
#   flag - the flag name including its leading `--` (never a line number)
#
# This file may only shrink as flags are removed. It must never grow to
# make room for a new secret-on-argv flag; cli_secret_flag_surface.rs
# enforces both directions. vigil-ha and vigil-bin hold zero entries today
# — that is the correct, observed starting point, not an oversight.

vigil/config.rs|--rtsp-password
